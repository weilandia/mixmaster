<h1><%= @artist.name %></h1>
<%= image_tag @artist.image_path %>
<%= link_to "Edit Artist", edit_artist_path(@artist.id) %>
<%= button_to "Delete Artist", artist_path(@artist.id), method: "delete" %>
