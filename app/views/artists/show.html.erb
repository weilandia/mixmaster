<h1><%= @artist.name %></h1>
<%= image_tag @artist.image_path %><br>
<%= link_to "Edit Artist", edit_artist_path(@artist.id) %><br>
<%= button_to "Delete Artist", artist_path(@artist.id), method: "delete" %><br>
<%= link_to "New Song", new_artist_song_path(@artist) %>
